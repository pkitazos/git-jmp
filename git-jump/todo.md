
- [ ] `git jump -` should work like `git switch -` it should not fuzzy match on some branch
- [ ] should be able to call `--version` from anywhere on the system (probably comes by default with clap)
